diff -ruN mpc-0.9-old/configure.ac mpc-0.9/configure.ac
--- mpc-0.9-old/configure.ac	2012-10-19 20:20:27.000000000 +0900
+++ mpc-0.9/configure.ac	2014-03-06 19:04:40.028982909 +0900
@@ -153,15 +153,15 @@
         [AC_LANG_SOURCE(
         [[
 #include "gmp.h"
-#if (__GNU_MP_VERSION*100 + __GNU_MP_VERSION_MINOR*10 + __GNU_MP_VERSION_PATCHLEVEL < 432)
-# error "Minimal GMP version is 4.3.2"
+#if (__GNU_MP_VERSION*100 + __GNU_MP_VERSION_MINOR*10 + __GNU_MP_VERSION_PATCHLEVEL < 421)
+# error "Minimal GMP version is 4.2.1"
 error
 #endif
         ]])],
         [AC_MSG_RESULT(yes)],
         [
         AC_MSG_RESULT(no)
-        AC_MSG_ERROR([GMP version >= 4.3.2 required])
+        AC_MSG_ERROR([GMP version >= 4.2.1 required])
         ])
 
 # Check for a recent MPFR: we require MPFR 2.4.2 so that the tests
